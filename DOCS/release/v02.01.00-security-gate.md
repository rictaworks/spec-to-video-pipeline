# セキュリティゲート判定結果 v02.01.00

| 項目 | 内容 |
|---|---|
| 対象 | rictaworks/spec-to-video-pipeline（リリース候補 v02.01.00） |
| 実施日 | 2026-08-25 JST |
| 実施環境 | Claude Desktop（リポジトリ側 CI では実施しません） |
| 基準 | `.claude/OWASP10.md`（OWASP Top 10 2021） |
| 判定 | 合格（是正1件を適用済み） |

## 1. 検査結果

| 検査 | 方法 | 結果 |
|---|---|---|
| 依存の脆弱性 | `npm audit`（本番・開発の双方） | 0 件 |
| 配布物への機微情報の混入 | `.skill` を展開し、鍵・トークン・パスワードの語形と鍵ブロックを走査 | 検出なし |
| リポジトリへの機微情報の混入 | 追跡対象の差分を走査 | 検出なし |
| 資格情報の取り扱い | `references/cost-and-credentials.md` の規定を確認 | 環境変数名のみを記載し、値はログ・台帳・レポート・エラー出力に残さない旨を明記 |
| 実行時の外部通信 | 開発環境の分岐（`scripts/lib/env.js`・`scripts/lib/stub/generation.js`） | 開発環境では生成 API を呼ばずスタブへ分岐。判定不能時は停止 |
| 個人情報 | 成果物・台帳・レポートの記述と雛形 | 実在の個人を特定できる情報の記載を禁じる旨を明記 |
| CI の権限 | ワークフローの `permissions` | **是正1件**（下記） |
| アクションの固定 | `uses:` の指定 | すべてコミットハッシュで固定済み |
| 配布物の同梱物 | `.skill` の内容一覧 | テスト・開発用スクリプト・`node_modules`・`.env` を含まない |

## 2. 是正した事項

| 事項 | 内容 | 対応 |
|---|---|---|
| CI の権限が未指定 | `.github/workflows/ci.yml` に `permissions` の指定がなく、既定の権限で実行されていました（A05 Security Misconfiguration） | `permissions: contents: read` を明示しました |

`release.yml` は `contents: write` を明示済みで、配布物の添付に必要な範囲に収まっています。

## 3. OWASP Top 10 に対する適用可否

本スキルはサーバー・データベース・セッション・認証を持たず、利用者の環境で利用者自身の資格情報により実行されます。適用の可否は次のとおりです。

| 番号 | 項目 | 扱い |
|---|---|---|
| A01 | アクセス制御の不備 | 非適用（アクセス制御の主体を持ちません） |
| A02 | 暗号処理の失敗 | 適用。資格情報を保存せず、環境変数から都度読みます |
| A03 | インジェクション | 適用。外部入力は引数として渡し、シェルへ文字列連結しません |
| A04 | セキュリティ設計の欠如 | 適用。承認ゲートと上限で課金の暴走を止めます |
| A05 | セキュリティ設定ミス | 適用。CI の権限を是正しました |
| A06 | 脆弱・旧版の依存 | 適用。`npm audit` 0 件 |
| A07 | 認証の欠陥 | 非適用（認証を持ちません） |
| A08 | 整合性の失敗 | 適用。アクションをハッシュで固定し、配布物にチェックサムを添えます |
| A09 | ログと監視の不足 | 適用。課金記録と検証記録を版管理へ残します |
| A10 | SSRF | 非適用（サーバー側リクエストを行いません） |

## 4. 判定

検出した設定不備1件を是正した状態で、リリースを妨げる指摘はありません。**リリース可**と判定します。
