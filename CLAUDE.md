# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリの位置づけ

`spec-to-video` エージェントスキル（製品版フルエディション）。動画設計書 Markdown を入力に、素材生成・ナレーション合成・字幕・Remotion 合成・コンプライアンス検証を経て mp4 と付随成果物を出力する**手順定義**。対象ホストは Claude Code / Codex / Antigravity の3ツール。**自社開発案件（B2B）**。

`requirements.md` 24節により、**サーバー・DB・セッション・認証・スケーラビリティ・Bot 判定・ハードウェア連携はいずれも非適用**。Web アプリでもデスクトップアプリでもなく、画面も API エンドポイントも持たない。したがって次の全社規約は本リポジトリに適用しない。

- ページ一覧・API 一覧・W3C・PageSpeed・OGP・Cookie 同意・特商法表記
- Next.js + Rails + PostgreSQL 構成、FastAPI / Gin の追加 API
- Vercel / Railway へのデプロイ、`rictaworks.jp` サブドメイン、バックエンドドメインの隠蔽
- 認証（Google ログイン / X ログイン）と一般消費者向けログイン導線
- マイクロサービス・MVC・API Gateway・メッセージングのアーキテクチャ選択
- `alert()` / `confirm()` / `prompt()` の使用禁止（UI を持たないため）

主要な制約は以下の通りです。

- **`requirements.md` が仕様の単一の正**。読み取り専用（444）で保護されている。**Claude 側から書き換えない。** 表記揺れの吸収は本ファイルか README 側で行う
- **PUBLIC リポジトリ**。コミット対象に入れてよいか判断してから `git add` する
- 対象範囲外：配信プラットフォームへのアップロード、動画設計書そのものの作成

### 現状（2026-08-24 JST）

追跡済みファイルは `README.md` のみ。成果物である `src/skills/spec-to-video/`（`SKILL.md` / `references/` / `scripts/` / `assets/`）は**未実装**。ビルド・テスト・lint のツールチェーンは未選定で、コマンドは存在しない。**推測でコマンドを書かない。** 選定・導入したら、その時点で本ファイルにコマンドを実測値で記録する。

## 成果物

**成果物は `src/skills/spec-to-video/` 配下のスキル一式**（`SKILL.md` / `references/` / `scripts/` / `assets/`）です。mp4 はスキルの利用者がそれぞれの環境で生成するものであり、本リポジトリの成果物ではありません。Remotion・生成 API・TTS も利用者の環境で動きます。

`requirements.md` 13節のパス表記（`references/...`・`scripts/...`・`assets/...`）は**スキルのルートからの相対**であり、リポジトリ上では `src/skills/spec-to-video/` を起点に読み替えます。配布時はこのディレクトリを `.skill` にまとめ、各ツールのスキルディレクトリへ symlink で設置します。

### 配布とユーザーテスト

- **配布は GitHub Releases**。`.skill`（zip）を Release に添付する。ホスティング先・ドメインを持たない
- **ユーザーテストは、Releases からダウンロードし、このデスクトップ（Claude Desktop）に設置して実行することをいう。** リポジトリの作業コピーを直接読ませて動かす確認は、ユーザーテストとして扱わない
- 手順は「Releases ページをブラウザで開く → zip をダウンロードする → スキルディレクトリへ展開・設置する → 動画設計書を渡して実行する → 出力を確認する」の流れで書く
- リポジトリ内の未リリースの状態で通ったことは、配布物で通ったことにならない。**タグ・添付物・設置手順まで含めて確認する**

## 参照資料の所在

| パス | 内容 | Git |
|---|---|---|
| `requirements.md` | 仕様の正（24節・工程 P1〜P16・設計要件12項目） | 追跡候補 |
| `MM/` | **動画開発時は必読。** Adobe Premiere メソッド一覧（トランジション・エフェクト・キーフレーム・合成/マスク・オーディオ・カラー・テキスト・テロップ設計・技術仕様） | ignore |
| `DOCS/TM.md` | テストメソッドとフレームワーク概要 | 追跡候補 |
| `DOCS/DP.md` | 開発原則（YAGNI / KISS / DRY / SOLID） | 追跡候補 |
| `DOCS/CRAP.md` | デザイン4原則。図解・テロップの配置判断に使う | 追跡候補 |
| `.claude/OWASP10.md` | OWASP Top 10（2021）。セキュリティレビューの基準 | ignore |
| `.claude/Manager.md` | Issue ベース並列開発の分割・依存・ready 判定ロジック | ignore |
| `.claude/TEST-HARNESS-SAFETY.md` | TH1〜TH5。自作テストランナーを書く・変える時に必ず確認 | ignore |

### `.claude/` の追跡範囲

`.claude/` は既定で除外し、**共有が必要なものだけを追跡する**（PUBLIC リポジトリのため内部運用資料を出さない）。

| 追跡する | 理由 |
|---|---|
| `.claude/settings.json` | フックはリポジトリに入っていないと他環境・CI で効かない |
| `.claude/hooks/` | 同上 |
| `.claude/agents/` | サブエージェント定義は開発者間で共有する |

上表以外（`Manager.md` / `auto-optimizer.md` / `OWASP10.md` / `QC10.md` / `CC.md` / `TEST-HARNESS-SAFETY.md` / `settings.local.json`）は除外する。`.env` と `MM/` も除外する。

## 文章とコンテンツの規約

- **利用者・視聴者が読むものは必ずですます調で書く。だである調を禁止する。** 対象は README・SKILL.md・`references/`・実行レポート・字幕・ナレーション原稿・図解文言・PR 本文・Issue 本文・リリースノート
- **日本語版のみを開発する。** 多言語対応を実装しない
- 絵文字を使わない
- プロ品質の文章が要る場面は `writer` サブエージェントに担当させる
- 画像は AI 生成とする（起点画像・図解画像・表紙相当の静止画）。素材サイトからの流用を既定にしない
- 連絡先に個人名を使わない。メールアドレスは `info@rictaworks.jp`
- 日時は JST、エンコードは UTF-8。図解は Mermaid（未導入。使用時に導入する）

`requirements.md` は原本であり、だである調のまま変更しない。本ファイルと `.claude/agents/` は Claude への作業指示であり、コンテンツには含めない。

## 開発フロー

### 全体の流れ

```
実装 → セキュリティレビュー → コミット・プッシュ → reviewer & pr-checker → マージ
  ↑                                                                          │
  └──────────────── Issue が残っている間は繰り返す ───────────────────────────┘
                                     ↓（全 Issue 完了後）
                    audit & security gate → release → ユーザーテスト
```

| 段 | 実施 | 強制 |
|---|---|---|
| 実装 | TDD（`plan -> red -> coding -> green`） | — |
| セキュリティレビュー | `/security-review`（基準は `.claude/OWASP10.md`）→ `record-review.sh security` | `pre-commit-security-review.sh` がコミットを止める |
| コミット・プッシュ | ブランチへ push し、PR を作成する | `main` への直接 push を禁止（`src/**`） |
| reviewer | Issue の受け入れ要件と各基準への適合を検証 → `record-review.sh reviewer <PR>` | `pre-merge-review.sh` がマージを止める |
| pr-checker | PR のタイトル・本文の日本語化とユーザーテスト手順の記述 → `record-review.sh pr-checker <PR>` | 同上 |
| マージ | `gh pr merge <PR番号>`（番号の明示が必須） | 上記2つの記録が揃うまで停止 |
| audit & security gate | **デスクトップ（Claude Desktop）で実施する。** リポジトリ側の CI に実装しない | 規程（バージョン・タグ・添付物）が無いと「判定不能」になる |
| release | 注釈付きタグ（`git tag -a`）を打ち、Release を発行する。CI が配布物を生成・添付する | — |
| ユーザーテスト | Releases から配布物をダウンロードし、デスクトップに設置して実行する | 作業コピーでの確認は不可 |

audit & security gate は**リリース候補ごとに実施する**。マージを繰り返している間は走らせない。


### TDD（厳守）

`plan -> red test -> coding -> green test` の順。テストを書く前に実装しない。テストフレームワークは実装言語の確定後に選定し、実行コマンドを本ファイルに記録する。

### ブランチと PR

- **`main` ブランチでの作業禁止**
- **`src/**` の変更は必ず PR を作成する。直接 push 禁止**
- `src/**` 以外（ドキュメント・`SPEC/`・`TASKS/` 等）は `main` への push を許可
- **PR のタイトルと本文は日本語で書く**
- PR 本文には**非エンジニア向けユーザーテスト手順**を丁寧に書く。**非エンジニアとはブラウザしか使わない者をいう**（ローカル環境の構築・コマンド実行を求めない粒度。記述は `pr-checker`、実行スクリプト化は `tester` が担当）

### フック（`.claude/settings.json` に設定済み・PreToolUse / Bash）

いずれも**判定できない場合は停止する**（フォールバックしない）。トレースは `HOOK_TRACE=1` で有効になる。

| スクリプト | 発動 | 通過条件 |
|---|---|---|
| `.claude/hooks/pre-commit-security-review.sh` | `git commit` | ステージ内容のダイジェストに対応する記録が `review-records/` にある |
| `.claude/hooks/pre-merge-review.sh` | `gh pr merge` / `git merge` | 対象コミットに対する reviewer と pr-checker の記録が**両方**ある |

記録は実施後に自分で残す。ステージ内容や対象コミットが変わると記録は自動的に無効になる。**記録は版管理対象の `review-records/` に残す**（第三者が実施を検証できるようにするため）。ダイジェストの算出からは `review-records/` を除外しているので、記録を追加してもステージ内容の判定は変わらない。

```bash
bash .claude/hooks/record-review.sh security          # /security-review 実施後
bash .claude/hooks/record-review.sh reviewer 12       # reviewer 実施後（12 は PR 番号）
bash .claude/hooks/record-review.sh pr-checker 12     # pr-checker 実施後
```

文言は `.claude/hooks/messages.conf` に分離してある。`gh pr merge` は PR 番号の明示が必須。

### CI/CD

- CI は必須。`requirements.md` 20節により、CI が担うのはスクリプトの実行検証とパッケージングまで
- **GitHub Releases の発行をトリガーに、配布物の生成と添付を CI で行う**
- **Release をトリガーとする audit と security gate はデスクトップ（Claude Desktop）側で実施する。** リポジトリ側の CI に監査とゲートを実装しない
- 各ツールのスキルディレクトリへの設置は人間の手動工程

## リリースとバージョン

**規程が無いと監査が「判定不能」を返して合格に届かない。** 以下を規程として維持する。

| 項目 | 規則 |
|---|---|
| バージョン番号 | `メジャー2桁.マイナー2桁.デバッグ2桁`。初期値は `01.01.00` |
| メジャー | 破壊的変更（入出力フォーマットの変更、工程の削除）。非推奨期間を設け、代替手段を告知する |
| マイナー | 発火条件・手順の分岐に影響する変更 |
| デバッグ | 参照情報の更新のみ、および不具合修正 |
| タグ形式 | **注釈付きタグ必須。最初から `git tag -a` で作成する**（`git tag` の軽量タグを作らない） |
| 必須添付物 | 配布パッケージ（`.skill`）、リリースノート、監査報告書、セキュリティゲート判定結果 |
| 検証・承認の記録 | **版管理対象のログとしてリポジトリに残す**（R8-02 対策）。ローカルや会話だけに残さない |

改訂時は配布済みバージョンとの差分を「発火条件に影響 / 手順の分岐に影響 / 参照情報の更新のみ」に分類して一覧化する（`requirements.md` 21.2）。

> `requirements.md` 21.2 はセマンティックバージョニングと記載しているが、本表の桁規則を優先する。原本は変更しない。

## AI API とモデル選定

- 使用するのは **gemini / nano banana / veo**
- **利用可能なモデルを都度調べ、要件を満たす最安値のモデルを選ぶ。** 単価調査 → 見積もり提示 → 実行の順とし、モデル・解像度・尺の引き上げは本人確認を取ってから行う
- モデル名・エンドポイント・許容クリップ長を**スキル本体に埋め込まない**（`requirements.md` 2.1）。動画設計書の「使用モデル」表と実行環境から取得し、素材台帳に記録する。上記の3系統は、その選定候補の範囲を定めるもの
- 資格情報は利用者の環境変数から読む。変数名のみを記載し、値は記録しない

## コード規約

- **車輪の再発明を避ける。** メンテナンスコストとセキュリティの観点から、実績のあるライブラリ・フレームワーク・OSS・SaaS を優先し、オリジナルコードを少なく保つ
- **フォールバック禁止。** 失敗を黙って握りつぶさず、例外処理を明示的に書く
- **デバッグトレースが追えるように書く**
- **制御構文・条件構文以外はクラスまたは関数に入れる**
- **グローバル変数禁止**（セキュリティ上の理由）
- **文字列リテラルは設定ファイルへ分離する。ハードコードを検出するテストを書く**
- **環境の判定を実装し、開発環境と本番環境で分岐できるようにする。** 開発環境では課金を伴う生成 API を呼ばず、スタブへ分岐させる（認証を持たないため、認証状態の分岐は非適用）
- 環境変数は `.env` を参照する。値をコード・台帳・ログ・レポートに書かない

## 動画成果物の規約

- **ナレーションとアニメーションの同期はカット単位で行う。** シーン単位の長いナレーションを1本で合成せず、カットに対応させて分割する
- **既定の話者は四国めたん（ノーマル）。** 設計書の `narration.speaker` が明示された場合はそれを優先する
- 字幕・図解文言・ナレーションはですます調
- 見せ方の設計は `MM/` と `DOCS/CRAP.md` に従う（`designer` サブエージェント）

## ディレクトリ規約

実装は `src/` 以下に置く。開発用スクリプト等は `src/` の外に配置する。

| ディレクトリ | 用途 |
|---|---|
| `src/skills/spec-to-video/` | **成果物のスキル本体。** `SKILL.md` / `references/` / `scripts/` / `assets/` を置く |
| `test/pr<番号>/` | PR ごとのテスト。対象は開発サーバー |
| `SPEC/` | 仕様書・リバースエンジニアリング成果物（ER図・DFD・シーケンス図・クラス図・状態遷移図・ユースケース図）を管理・更新 |
| `TASKS/` | タスク（Git 追跡外・ローカル管理） |
| `DEBUG/` | バグ報告（Git 追跡外・ローカル管理） |
| `CLIENT/` | クライアント要望（Git 追跡外・ローカル管理） |
| `WORK/` | 作業報告（Git 追跡外・ローカル管理） |
| `ENV/DEVELOPMENT.md` `ENV/PRODUCTION.md` | 開発環境・本番環境（Git 追跡外・ローカル管理） |
| `DELETE/` | ゴミ箱（Git 追跡外・ローカル管理） |

**`README.md` と `SPEC/` に未実装のものを書かない。**

## サブエージェント

`.claude/agents/` に定義済み（11本）。

| エージェント | 担当 |
|---|---|
| `director` | 設計要件12項目に基づく裁定。スコープの取捨 |
| `project-manager` | Issue 分割・依存設計・ready 判定（`.claude/Manager.md` 準拠） |
| `designer` | テロップ・図解・カラー・カット構成の設計（`MM/` 必読） |
| `debugger` | 再現・原因特定・`DEBUG/` への起票 |
| `tester` | PR のユーザーテスト手順の実行スクリプト化、`DOCS/TM.md` に沿ったテスト作成（`test/pr<番号>/`） |
| `data-scientist` | 測定軸（23節）の集計と分析 |
| `deployer` | パッケージング・タグ・バージョン差分 |
| `writer` | SKILL.md・`references/`・`SPEC/` の執筆 |
| `service-manager` | 保守・監視（21節・22節） |
| `reviewer` | マージ前コードレビュー。Issue の受け入れ要件と CC / OWASP10 / QC10 / CRAP / DP / TM への適合を検証（フックが記録を要求） |
| `pr-checker` | PR のタイトル・本文の日本語化と非エンジニア向けユーザーテスト手順の記述。レビューはしない（フックが記録を要求） |

## 削除操作の禁止

- 削除コマンド（`rm` / `rm -rf` / `rmdir` / `unlink` / `git clean -df` / `rsync --delete` / `find -delete` 等）を**生成しない**
- 削除が必要な場合は `DELETE/` へ移動するか、手動削除を依頼する説明に留める。削除の推奨・自動判断も行わない

## セキュリティ

- **コミット前に必ずセキュリティレビューを実行する**（基準は `.claude/OWASP10.md`）
- シークレットは環境変数で渡す。`.gitignore` への追加を先に行い、コミット前に `git status` でステージングを確認する
- 生成 API の資格情報は利用者の環境変数から読む。値は台帳・ログ・レポート・成果物のいずれにも記録しない（`requirements.md` 15節）
- 成果物・台帳・レポートに実在の個人を特定できる情報を記載しない。画面内にも表示しない

## コスト管理（本スキル固有の重要制約）

`requirements.md` 16節・18節に基づく。実装時に崩さないこと。

- 生成呼び出しは `scripts/generate_asset` の**単一窓口**のみ。直接呼び出し経路を作らない
- 素材単位のリトライ上限とセッション単位のハードキャップを**分離**して管理し、到達時は生成を停止して台帳と課金記録を確定する
- 課金と不可逆な生成を伴う工程（P8 起点画像 / P9 クリップ / P10 図解）の直前に**承認ゲート**を置く。発火のみでは生成もレンダリングも開始しない
- **素材台帳が単一の正**。採用済み素材は再実行時に再生成しない
- 再レンダリングで解消できる不適合に再生成を選ばない（差し戻し先は検出区間から決定・11節）
- 設計書から抽出できない必須項目は**推測補完せず、欠損を列挙して停止**する

## 権利

- 使用するモデルの利用規約で商用利用可否を確認し、確認日を `docs/references.md` に記録する
- 実在の人物・ロゴ・他社著作物の生成回避を記述に明示し、映り込んだ素材は採用しない
